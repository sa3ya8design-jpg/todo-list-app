# MVP仕様書

## 1. 概要

### コンセプト

> 最短で入力し、今やるべきことだけに集中できる個人向けToDo管理ツール

シンプルで迷わない操作性を重視し、MVPでは必要最低限の機能のみ実装する。

---

# 2. 技術構成

- フロントエンド：Streamlit（Python）
- バックエンド：Python（Streamlitアプリ内で完結）
- データ保存：Google スプレッドシート（課題要件。projects / tasks の2ワークシート）
  - 認証はサービスアカウント方式（公開サーバーでも動作する）
  - スプレッドシートIDと鍵は `.streamlit/secrets.toml` に設定する
    （公開時はホスティング側のSecrets設定に同じ内容を入れる）
  - Sheets APIの遅延対策として、読み取りは60秒キャッシュし書き込み時に破棄する
- 認証：なし（個人利用の単一ユーザー前提のためMVP対象外）

---

# 3. データモデル

## Project


| 項目         | 型        |
| ---------- | -------- |
| id         | UUID     |
| name       | string   |
| color      | string   |
| isArchived | boolean  |
| createdAt  | datetime |
| updatedAt  | datetime |


---

## Task


| 項目          | 型                   |
| ----------- | ------------------- |
| id          | UUID                |
| projectId   | UUID                |
| title       | string              |
| memo        | string              |
| priority    | high / medium / low |
| dueDate     | date（任意）            |
| completed   | boolean             |
| completedAt | datetime（任意）        |
| sortOrder   | number              |
| createdAt   | datetime            |
| updatedAt   | datetime            |


---

# 4. 画面一覧

MVPでは以下の画面を実装する。

1. ホーム
2. プロジェクト一覧
3. プロジェクト詳細
4. 完了プロジェクト一覧（アーカイブ）

サイドバーには画面ナビゲーションに加え、進行中のプロジェクト一覧を表示する。
クリックでそのプロジェクト詳細へ移動する。

※ログイン画面は個人利用の単一ユーザー前提のためMVP対象外とする。

モーダル

- タスク作成・編集
- プロジェクト作成・編集

---

# 5. ホーム画面

役割

未完了タスクの中から「今やるべきこと」を判断する画面。

## 表示対象

未完了タスクのみ。

タスク行には、どのプロジェクトのタスクか分かるように
プロジェクト名（カラー付き）を表示する。

## 表示順

1. 優先度（高→中→低）
2. 期限
3. 作成日時

## グループ

表示順は以下とする。

- 期限切れ
- 今日
- 明日
- 今週
- 今月
- それ以降
- 期限なし

タスクが存在しないグループは表示しない。

## 今日のタスクが0件の場合

以下のメッセージを表示する。

> 今日のタスクはありません

## 完了したタスク

画面下部に

> 完了したタスク（件数）

を折りたたみ表示する。誤って完了にしたタスクをここからすぐ未完了へ戻せる。

---

# 6. プロジェクト一覧

表示内容

- プロジェクト名
- プロジェクトカラー

表示しないもの

- 未完了件数
- 進捗率

---

# 7. プロジェクト詳細

表示対象

そのプロジェクトの未完了タスク。

一覧表示項目

- タイトル
- 優先度
- 期限

メモ本文は一覧に表示しない。

画面下部に

> 完了したタスク（件数）

を折りたたみ表示する。

展開すると完了タスク一覧を表示し、未完了へ戻せる。

---

# 8. タスク

## 必須項目

- タイトル

## 任意項目

- プロジェクト
- 期限
- メモ

## 優先度

選択肢

- 高
- 中
- 低

初期値は「中」。

---

# 9. タスク編集

編集はモーダルで行う。

画面遷移はしない。

## 保存

「保存」ボタンで保存し、モーダルを閉じる。

- 自動保存は行わない
  （自動保存だと保存されたか分かりづらく、結局「閉じる」操作も必要になるため。
  「保存」ボタンなら保存と閉じるを1クリックで兼ねられる）
- 「キャンセル」で保存せずに閉じる
- タイトル未入力で保存した場合はエラーを表示する

プロジェクト作成・編集モーダルも同じ方式とする。

---

# 10. タスク操作


| 操作         | 動作     |
| ---------- | ------ |
| チェックボックス   | 完了     |
| タスクをタップ    | 編集モーダル |
| 「延期」ボタン    | 延期     |
| 編集モーダルの「削除」 | 削除（確認あり） |

完了時はトースト通知で「完了しました」と知らせ、戻し方（『完了したタスク』）を案内する。

※スワイプ操作はStreamlit標準機能では実現できないため、「延期」ボタンに置き換える。
※「完了」はチェックボックスと重複するため、ボタンは追加せずチェックボックスのみとする。


延期メニュー

- 明日
- 今週末
- 来週
- 日付選択

---

# 11. プロジェクト

保持情報

- 名前
- カラー

カラーは8色のプリセット（レッド・オレンジ・イエロー・グリーン・ブルー・パープル・ブラウン・グレー）から選択する。
自由なカラーコード入力は行わない。

対応機能

- 作成
- 編集
- 完了（アーカイブ）
- 削除（中のタスクごと削除。確認あり）

サブプロジェクトやグループ機能は実装しない。

---

# 12. 完了プロジェクト（アーカイブ）

終わったプロジェクトは「完了プロジェクト」へ移動する（内部的にはアーカイブ）。
UI上の表記は「アーカイブ」ではなく「完了プロジェクト」「完了にする」「進行中に戻す」を用いる。

完了にした後も

- タスク
- 完了履歴

は保持し、いつでも進行中に戻せる。

---

# 13. フィルター

プロジェクト詳細画面ではクイックフィルターを実装する。

対象

- すべて
- 今日
- 期限切れ
- 高優先度

---

# 14. 今回実装しない機能

以下はMVP対象外とする。

- サブタスク
- タグ
- ラベル
- 通知
- 繰り返しタスク
- 添付ファイル
- コメント
- カレンダー表示
- カンバン表示
- ガントチャート
- ダークモード
- オフライン対応
- 複数ユーザー対応
- 権限管理

---

# 15. 実装時の原則

- 操作回数を最小限にする
- 入力項目を最小限にする
- 一覧性を優先する
- 過剰設計を避ける
- 保守しやすいコードを書く
- 仕様変更は実装前に提案する

