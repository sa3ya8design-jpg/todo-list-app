# [CLAUDE.md](http://CLAUDE.md)

## プロジェクト概要

このプロジェクトは、個人利用を目的としたToDo管理ツールです。

コンセプトは、

> **「最短で入力し、今やるべきことだけに集中できること」**

です。

一般的な多機能タスク管理ツールではなく、シンプルで高速な操作性を重視したMVPを開発します。

---

# 開発方針

実装中は以下の方針を最優先してください。

- MVPを完成させることを最優先とする
- 操作回数を減らすことを重視する
- 一覧性を重視し、情報を増やしすぎない
- シンプルで保守しやすい実装を心掛ける
- 過剰設計（YAGNI）は避ける

---

# 仕様について

機能仕様は `docs/mvp-spec.md` を正とします。

仕様に記載されていない機能は実装しないでください。

より良い実装方法を思いついた場合は、勝手に仕様を変更せず、まず提案してください。

---

# 実装ルール

- TypeScriptを使用する
- 型安全を意識する
- 可読性を重視する
- コンポーネントは責務ごとに分割する
- 重複コードは適切に共通化する
- 不要な抽象化は行わない

---

# UI/UX方針

このアプリでは以下を重要視します。

- 入力はできるだけ速く
- 一覧はできるだけシンプルに
- 編集はできるだけ少ない操作で完了する
- ユーザーに余計な判断をさせない

---

# 実装中の判断基準

迷った場合は、以下の優先順位で判断してください。

1. シンプルであること
2. 操作回数が少ないこと
3. 保守しやすいこと
4. 将来拡張しやすいこと

---

# 今回は実装しない機能

以下はMVP対象外です。

- サブタスク
- タグ
- ラベル
- 通知
- 繰り返しタスク
- カレンダー表示
- カンバン表示
- 複数ユーザー対応
- ファイル添付
- コメント機能
- ダークモード

必要になった場合は、MVP完成後に追加を検討してください。